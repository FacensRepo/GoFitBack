# Configurações de Ambiente para Deploy no Render

# === Database ===
# URL de conexão do PostgreSQL (formato Ecto)
# Obtenha do seu banco PostgreSQL no Render e troque postgres:// por ecto://
DATABASE_URL=ecto://user:password@host/database

# Tamanho do pool de conexões (padrão: 10)
POOL_SIZE=10

# IPv6 (geralmente não necessário no Render)
# ECTO_IPV6=false

# === Phoenix ===
# Secret key base para criptografia de cookies e sessões
# Gere com: mix phx.gen.secret
SECRET_KEY_BASE=your_secret_key_base_here

# Token de assinatura
# Gere com: mix phx.gen.secret
TOKEN_SIGNING_SECRET=your_token_signing_secret_here

# Host da aplicação (seu domínio no Render)
PHX_HOST=seu-app.onrender.com

# Porta do servidor (Render usa 10000 por padrão)
PORT=10000

# Habilitar servidor Phoenix
PHX_SERVER=true

# === Ambiente ===
# Ambiente de execução
MIX_ENV=prod

# === Opcional ===
# Query do DNS cluster (geralmente não necessário)
# DNS_CLUSTER_QUERY=
